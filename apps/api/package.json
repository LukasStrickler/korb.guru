{
  "name": "@korb/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --host 0.0.0.0 --port ${API_PORT:-8001} --reload",
    "lint": "uv run ruff check src scripts",
    "typecheck": "uv run python -m compileall src scripts",
    "test": "uv run pytest"
  }
}
